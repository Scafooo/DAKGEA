# PLM Augmentation - No BART Fine-tuning
# Structural expansion only (no language model interpolation)

augmentation:
  method: plm_augmentation
  max_depth: 2
  ratio: 0.5

  # Disable BART fine-tuning
  bart:
    enable_finetuning: false      # Skip BART fine-tuning entirely

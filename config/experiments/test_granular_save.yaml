name: test_granular_save

dataset: D_W_15K_V1

reduction:
  ratio: 0.3
  method: random_entities
  save_dataset: true    # Save reduced dataset
  save_model: false     # Don't save reduction model
  eval: false

augmentation:
  method: llm_conceptnet
  save_dataset: false   # Don't save augmented dataset
  save_model: true      # Save augmentation model
  eval: false

model: bert_int

clear: true  # Test that clear respects the save flags

overwrite_existing: true
